# the_data_science_app
A web application to automate data pre-processing, feature engineering, and ML modelling tasks. Accepts a user's dataset as input and directs them through the data science workflow.
